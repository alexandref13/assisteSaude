import 'package:flutter/material.dart';

class TerapiasWidget extends StatelessWidget {
  const TerapiasWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Terapias'),
        ),
      ),
    );
  }
}
